# LIM Security Protocol Workspace Implementation

- `[x]` Obtain absolute file coordinates for `prompts` and `payloads` directories.
- `[x]` **Backend**: Construct FastAPI endpoints in Python for directory traversal (`/v1/lim/prompts`, `/v1/lim/payloads`).
- `[x]` **Backend**: Build execution endpoint (`POST /v1/lim/save`) ensuring output files are stamped as `payload-filename-done.YY-MM-DD-HHMM.md`.
- `[ ]` **Frontend**: Tear down current Campaign layout and construct the 4-Pane LIM Architecture.
- `[ ]` **Frontend**: Box 1/Box 2 state machine logic (Staging Mode vs Execution Mode).
- `[ ]` **Frontend**: Hook React components into the new FastAPI endpoints.
- `[ ]` Verify execution writes and dashboard tracking speeds.
